# docker-on-amazon-linux2-ec2-webapp
Deploy Docker on Amazon Linux 2 EC2 and run a web application inside a container. This project includes instance setup, Amazon Linux 2 AMI selection, Docker installation, security group configuration for SSH/HTTP, Apache server deployment in a Docker container, and browser-accessible testing.
